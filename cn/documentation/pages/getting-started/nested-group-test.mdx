---
title: 嵌套组测试
description: 验证增量同步中嵌套组位置修复的第二个测试
---

<Note> ⚠️ 本文档由 AI 自动翻译。如有任何不准确之处，请参考[英文原版](../../../../en/documentation/pages/getting-started/nested-group-test.mdx)。</Note>

# 嵌套组测试

这是创建的第二个测试文件，用于验证嵌套组位置修复是否正常工作。

## 目的

此测试专门验证文件被添加到下拉菜单顶层而不是嵌套组内部问题的修复。

### 预期结果

翻译后，此文件应出现在：

**中文**:
```
"文档" dropdown
  └─ "快速开始" group
     └─ cn/documentation/pages/getting-started/nested-group-test
```

**日文**:
```
"ドキュメント" dropdown
  └─ "はじめに" group
     └─ jp/documentation/pages/getting-started/nested-group-test
```

### 不应该是这样（错误情况）：

```
"文档" dropdown
  ├─ "快速开始" group
  └─ cn/documentation/pages/getting-started/nested-group-test  ← 错误！在组外部
```

## 修复方案

新的 `add_page_at_location()` 方法：
1. 使用来自 `find_file_in_dropdown_structure()` 的 `file_location` 路径
2. 导航嵌套组：`["pages", 0, "pages", 0, "pages", N]`
3. 将文件添加到正确的嵌套位置
4. 在目标语言中保留现有组名

## 测试验证

✅ 文件已添加到"快速开始"组内部
✅ 文件未在下拉菜单顶层
✅ 组名已保留（"快速开始"、"はじめに"）
✅ 下拉菜单名已保留（"文档"、"ドキュメント"）

---

🤖 嵌套组位置修复的第二个测试