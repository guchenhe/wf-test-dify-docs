---
title: 测试编辑文件 2（增强版）
description: 第二个测试文件，包含增强内容，用于验证多文件编辑
---

<Note> ⚠️ 本文档由 AI 自动翻译。如有任何不准确之处，请参考[英文原版](/en/documentation/pages/getting-started/test-edit-2)。</Note>

# 测试编辑文件 2（增强版）

这是第二个测试文件的**增强版本**，包含新的章节。

## 更新的内容

内容修改包括新的章节、代码示例和表格。

### 代码示例

```javascript
// Example code that should be preserved in translation
function translateContent(source, target) {
  return workflow.translate({
    from: source,
    to: target,
    mode: 'streaming'
  });
}
```

### 对比表格

| 功能 | 之前 | 之后 |
|---------|--------|-------|
| 翻译 | 完整 | 增量 |
| 上下文 | 无 | Git diff |
| 速度 | 慢 | 快 |

## 附加增量内容

本节演示第二轮增量更新：
- **性能**：每次增量更新应比完整翻译更快
- **质量**：翻译质量应在更新过程中保持一致
- **上下文保留**：先前的翻译提供了宝贵的上下文

### 实际使用场景

在实践中，文档会频繁更新。工作流必须处理：
1. 错别字修复和澄清
2. 新功能文档
3. 更新的示例和最佳实践
4. 结构重组