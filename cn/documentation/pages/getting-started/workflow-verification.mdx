---
title: 工作流验证测试
description: 验证基于统一配置的翻译工作流是否正常工作
---

<Note> ⚠️ 本文档由 AI 自动翻译。如有任何不准确之处，请参考[英文原版](../../../../en/documentation/pages/getting-started/workflow-verification.mdx)。</Note>

# 工作流验证测试

本文档验证在语言配置重构后，自动翻译工作流是否正常运行。

## 测试内容

在合并统一配置重构后，此 PR 测试：

### ✅ 配置加载
- 工作流从配置中加载 `source_language` 和 `target_languages`
- 不使用硬编码的语言值
- 翻译通知来自配置

### ✅ 翻译流程
- 英语 → 中文翻译执行
- 英语 → 日语翻译执行
- 两者都使用配置中的语言名称（`"English"`、`"Chinese"`、`"Japanese"`）

### ✅ 文件结构
- 翻译文件创建在基于配置的路径：
  - `cn/documentation/pages/getting-started/workflow-verification.mdx`
  - `jp/documentation/pages/getting-started/workflow-verification.mdx`

### ✅ 翻译通知
- AI 通知从 `config.json` 的 `translation_notice` 字段插入
- 英文版本的相对路径正确
- 通知格式与配置模板匹配

### ✅ 导航同步
- 所有语言部分的 `docs.json` 已更新
- 文件在所有语言中出现在相同位置
- 导航结构已保留

## 配置验证

工作流应使用 `tools/translate/config.json` 中的这些值：

```json
{
  "source_language": "en",
  "target_languages": ["cn", "jp"],
  "languages": {
    "en": {
      "name": "English",
      "directory": "en"
    },
    "cn": {
      "name": "Chinese",
      "directory": "cn",
      "translation_notice": "<Note>...</Note>"
    },
    "jp": {
      "name": "Japanese",
      "directory": "jp",
      "translation_notice": "<Note>...</Note>"
    }
  }
}
```

## 成功标准

✅ 自动创建翻译 PR
✅ 中文文件存在且包含正确的通知
✅ 日语文件存在且包含正确的通知
✅ 所有语言的 docs.json 已同步
✅ 日志中没有硬编码语言错误
✅ 工作流完成且无配置错误

如果所有标准都通过，重构就成功了！🎉