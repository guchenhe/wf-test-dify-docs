---
title: 重命名测试 Alpha
description: 对所有三个重命名修复的完整验证
---

<Note> ⚠️ 本文档由 AI 自动翻译。如有任何不准确之处，请参考[英文原版](/en/documentation/pages/nodes/rename-test-alpha)。</Note>

# 重命名测试 Alpha

此测试验证了重命名错误的所有三个关键修复：

## 三个修复

**修复 #1 - 扩展名剥离（位置查找）**：
- 问题：`_handle_rename` 查找 `"en/.../file.mdx"` 但 docs.json 中为 `"en/.../file"`
- 解决方案：在位置查找前剥离 `.mdx`/`.md`
- 提交：8bf899c6

**修复 #2 - 条件重命名检测**：
- 问题：Reconcile 总是跳过重命名检测，将 R100 重命名视为删除+添加
- 解决方案：仅在 git 实际检测到重命名时跳过
- 提交：532e97cf

**修复 #3 - 扩展名剥离（新目标）**：
- 问题：添加的条目带有扩展名，如 `"cn/.../file.mdx"` 而非 `"cn/.../file"`
- 解决方案：在添加到 docs.json 前剥离扩展名
- 提交：b9489a0a

## 测试计划

1. **Alpha**（本文件）→ 翻译为 cn/jp
2. **Alpha → Beta** → 验证物理文件和 docs.json 条目均已重命名
3. **Beta → Gamma** → 验证多次重命名的一致性

## 预期结果

每次重命名后：
- ✅ 物理文件已重命名：`cn/rename-test-*.mdx`、`jp/rename-test-*.mdx`
- ✅ docs.json 条目已更新：`cn/.../rename-test-*`（无扩展名）
- ✅ 无 cn/jp 条目的删除
- ✅ 条目与英文结构完全匹配

🤖 由 Claude Code 生成，用于完整的重命名修复验证