---
title: 移動テストファイル（再移動 + 編集済み）
description: Buildグループに再度移動され、さらに更新されたファイル
---

<Note> ⚠️ このドキュメントはAIによって自動翻訳されています。不正確な部分がある場合は、[英語版](/en/documentation/pages/getting-started/test-move-file)を参照してください。</Note>

# 移動テストファイル（再移動 + 編集済み）

このファイルは複数回移動されました：
1. **最初の移動**: Getting Started → ノード（コミット2）
2. **2回目の移動**: ノード → Build（コミット3、このコミット）

コンテンツは、この増分コミットで**さらに更新**されました。

## 移動 + 編集の組み合わせのテスト

これは、ワークフローの以下の機能をテストします：
- docs.json内の`group_path`の変更を通じた位置変更の検出
- インデックスベースのナビゲーションを使用した対応するcn/jpファイルの検索
- すべての言語バージョンへの同じ移動の適用
- 構造的変更と同時にコンテンツ更新を処理

## 現在の場所

- **初期グループ**: Getting Started（コミット1）
- **最初の移動**: ノード（コミット2）
- **2回目の移動**: Build ✅（コミット3、現在）
- **コンテンツステータス**: 増分変更により再度更新済み

## 更新されたコンテンツ

### 精密な調整

移動検出は以下のように機能します：
1. ベースコミットとHEAD間の`group_path`を比較
2. ファイルの場所が変更された時を検出（異なるグループパス）
3. インデックスベースのナビゲーション（グループ名ではない）を使用してcn/jpファイルを特定
4. すべての言語で新しい場所にファイルを移動

### インデックスベースのナビゲーション

グループは翻訳された名前ではなく、位置インデックスで照合されます：
- 英語: "Nodes"（インデックス1）
- 中国語: "节点"（インデックス1）- 同じ位置
- 日本語: "ノード"（インデックス1）- 同じ位置

これにより、翻訳間で移動が機能します。

### コンテンツ翻訳

コンテンツ更新は、コンテキストを考慮した翻訳を通じて適用されます：
```python
def move_and_translate():
    # Move file structurally
    move_file(old_location, new_location)
    # Translate updated content
    translate_with_context(file, existing_translation, git_diff)
```

## 検証

- [ ] cn/jp docs.jsonでファイルがノードグループに移動されている
- [ ] 物理ファイルは同じディレクトリに残る（docs.jsonのみが変更される）
- [ ] 更新されたコンテンツがcn/jpに翻訳されている
- [ ] インデックスベースのナビゲーションが正しく機能している

## 増分移動テスト（コミット3）

この3回目のコミットは、増分移動操作を検証します：
- **以前の状態**: ファイルは既にノードに移動され、翻訳済み
- **現在の操作**: 再度移動（ノード → Build）+ コンテンツ編集
- **期待される動作**:
  - 精密な調整が2回目の移動を検出
  - cn/jpエントリがdocs.json内のノードからBuildグループに移動
  - コンテンツ変更が増分的に翻訳される
  - インデックスベースのナビゲーションがBuildグループ内の正しいcn/jp位置を検出

### なぜ複数回の移動をテストするのか？

ドキュメント構造はしばしば進化します：
- 初期配置（適合しない可能性がある）
- 最初の再編成（より良いが、完璧ではない）
- 最終的な最適化（最も明確な構造）

ワークフローは、以前の履歴に関係なく、各移動を正しく処理する必要があります。