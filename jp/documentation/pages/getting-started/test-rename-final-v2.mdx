---
title: 名前変更テスト最終版 V2（再度名前変更 + 編集済み）
description: このファイルは再度名前が変更され、3回目のコミットでさらに更新されました
---

<Note> ⚠️ このドキュメントはAIによって自動翻訳されています。不正確な部分がある場合は、[英語版](/en/documentation/pages/getting-started/test-rename-final-v2)を参照してください。</Note>

# 名前変更テスト最終版 V2（再度名前変更 + 編集済み）

このファイルは複数回の名前変更を経ています：
1. **最初の名前変更**: test-rename-original.mdx → test-rename-final.mdx（コミット2）
2. **2回目の名前変更**: test-rename-final.mdx → test-rename-final-v2.mdx（コミット3、このコミット）

コンテンツはこの3回目のコミットで**さらに更新**されました。

## 名前変更 + 編集の組み合わせテスト

これはワークフローの以下の機能をテストします：
- ファイルの名前変更を検出する（同じ場所での削除 + 追加）
- cn/jpバージョンの名前変更時にファイル拡張子（.mdx）を保持する
- 名前変更と同時にコンテンツの変更を処理する
- 構造的な更新（名前変更）とコンテンツの更新（翻訳）の両方を適用する

## 更新されたコンテンツ

### 新しいセクション

このコンテンツは完全に新しいものであり、名前変更操作と共にcn/jpに翻訳される必要があります。

### コード例

```javascript
function testRenameAndEdit() {
  return {
    operation: 'rename',
    oldName: 'test-rename-original',
    newName: 'test-rename-final',
    contentModified: true
  };
}
```

### 重要な注意事項

<Note>
外科的調整システムは以下を行う必要があります：
1. 削除 + 追加パターンによって名前変更を検出する
2. 対応するcn/jpファイルの名前を変更する（.mdx拡張子を保持）
3. すべての言語のdocs.jsonエントリを更新する
4. 更新されたコンテンツを文脈に応じて翻訳する
</Note>

## 検証

- [ ] cn/jpディレクトリでファイル名が変更されている
- [ ] 拡張子が保持されている（.mdx）
- [ ] コンテンツが更新と共に翻訳されている
- [ ] docs.jsonが正しく同期されている

## 段階的な名前変更（コミット3）

この3回目のコミットは段階的な名前変更操作をテストします：
- **以前の状態**: ファイルはコミット2で既に名前変更され翻訳済み
- **現在の操作**: 再度名前変更 + コンテンツ編集
- **期待される動作**:
  - 外科的調整が2回目の名前変更を検出する
  - cn/jpファイルがtest-rename-finalからtest-rename-final-v2に名前変更される
  - コンテンツの変更が段階的に翻訳される
  - すべてのdocs.jsonエントリが正しく更新される

### なぜ複数回の名前変更をテストするのか？

実際のドキュメントは多くの場合、複数の反復を経ます：
- 初期命名（間違っている可能性がある）
- 最初の修正（まだ理想的ではない）
- 最終的な改善（より明確に）

ワークフローは各名前変更を独立して正しく処理する必要があります。