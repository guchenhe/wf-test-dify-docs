---
title: リネームテスト最終版（リネーム + 編集済み）
description: このファイルはリネームされ、同時にコンテンツが更新されました
---

<Note> ⚠️ このドキュメントはAIによって自動翻訳されています。不正確な部分がある場合は、[英語版](/en/documentation/pages/getting-started/test-rename-final)を参照してください。</Note>

# リネームテスト最終版（リネーム + 編集済み）

このファイルは **test-rename-original.mdx から test-rename-final.mdx にリネームされ**、同じコミットでコンテンツが**大幅に更新されました**。

## リネーム + 編集の組み合わせのテスト

これはワークフローの以下の機能をテストします：
- ファイルのリネームを検出する（同じ場所での削除 + 追加）
- cn/jp バージョンのリネーム時にファイル拡張子（.mdx）を保持する
- リネームと同時にコンテンツの変更を処理する
- 構造的な更新（リネーム）とコンテンツの更新（翻訳）の両方を適用する

## 更新されたコンテンツ

### 新しいセクション

このコンテンツは完全に新しいもので、リネーム操作と共に cn/jp に翻訳される必要があります。

### コード例

```javascript
function testRenameAndEdit() {
  return {
    operation: 'rename',
    oldName: 'test-rename-original',
    newName: 'test-rename-final',
    contentModified: true
  };
}
```

### 重要な注意事項

<Note>
外科的調整システムは以下を行う必要があります：
1. 削除 + 追加パターンを通じてリネームを検出する
2. 対応する cn/jp ファイルをリネームする（.mdx 拡張子を保持）
3. すべての言語の docs.json エントリを更新する
4. 更新されたコンテンツを文脈に応じて翻訳する
</Note>

## 検証

- [ ] cn/jp ディレクトリでファイルがリネームされている
- [ ] 拡張子が保持されている（.mdx）
- [ ] コンテンツが更新と共に翻訳されている
- [ ] docs.json が正しく同期されている